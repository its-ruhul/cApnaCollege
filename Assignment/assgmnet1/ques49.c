#include<stdio.h>

//error: semicolon placed instead of comma.
//fixed

//error: wrong quotation marks used.
//fixed

void main()
 {
    int x = 40, y = 30, z = 80;
    
    if(x < y < z)
        printf("\n Hello world");
    else
        printf("\nGood by");
 }